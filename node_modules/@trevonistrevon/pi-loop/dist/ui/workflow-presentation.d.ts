import { type ToolDisplayDetails, type ToolDisplayTone } from "../tools/tool-result.js";
import type { LoopEntry } from "../types.js";
export type WorkflowActivityStatus = "running" | "paused" | "idle" | "stopped";
export interface WorkflowActivity {
    status: WorkflowActivityStatus;
    statusSince: number;
    activityMs: number;
    workflowAgeMs: number;
    stateAgeMs: number;
}
export declare function formatCompactWorkflowDuration(ms: number): string;
export declare function deriveWorkflowActivity(entry: LoopEntry, now?: number): WorkflowActivity;
export declare function workflowActivityLabel(entry: LoopEntry, now?: number): string;
export declare function workflowTimingLines(entry: LoopEntry, now?: number): string[];
export declare function workflowTimingSummaryLine(entry: LoopEntry, now?: number): string;
export declare function workflowAttemptLabel(entry: LoopEntry): string;
export declare function workflowLeaseLabel(entry: LoopEntry, now?: number): string;
interface WorkflowDisplayOptions {
    entry: LoopEntry;
    action: string;
    tone: ToolDisplayTone;
    summary: string;
    extra?: string[];
}
export declare function workflowDisplayDetails({ entry, action, tone, summary, extra, }: WorkflowDisplayOptions): ToolDisplayDetails;
export declare function formatWorkflowInspection(entry: LoopEntry): string;
export {};
