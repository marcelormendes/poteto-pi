import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";
import type { TaskEntry, TaskStatus } from "../task-types.js";
export type NativeTaskEventName = "tasks:created" | "tasks:started" | "tasks:completed" | "tasks:closed" | "tasks:reopened" | "tasks:updated" | "tasks:deleted";
export interface NativeTaskEventPayload {
    taskId: string;
    subject: string;
    description: string;
    status: TaskStatus;
    previousStatus?: TaskStatus;
    createdAt: number;
    updatedAt: number;
    completedAt?: number;
    closedAt?: number;
    metadata?: Record<string, unknown>;
}
export declare function emitNativeTaskEvent(pi: ExtensionAPI, name: NativeTaskEventName, entry: TaskEntry, previousStatus?: TaskStatus): void;
