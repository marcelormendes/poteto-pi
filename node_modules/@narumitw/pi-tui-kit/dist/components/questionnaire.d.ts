import type { KeybindingsManager, Theme } from "@earendil-works/pi-coding-agent";
import { type Component, type Focusable, type TUI } from "@earendil-works/pi-tui";
import type { QuestionnaireAnswer, QuestionnaireLabels, QuestionnaireQuestion } from "../questionnaire.js";
import type { MenuCloseReason } from "../types.js";
export type QuestionnaireInteractionValue<QuestionId extends string> = {
    kind: "submitted";
    answers: QuestionnaireAnswer<QuestionId>[];
} | {
    kind: "closed";
    reason: MenuCloseReason;
};
export interface QuestionnaireComponentOptions<QuestionId extends string> {
    questions: readonly QuestionnaireQuestion<QuestionId>[];
    allowCustomAnswer: boolean;
    allowNotes: boolean;
    maxTextLength?: number;
    labels: QuestionnaireLabels;
    tui: TUI;
    theme: Theme;
    keybindings: KeybindingsManager;
    isCurrent(): boolean;
    onDone(value: QuestionnaireInteractionValue<QuestionId>): void;
}
export declare class QuestionnaireComponent<QuestionId extends string> implements Component, Focusable {
    private readonly options;
    private readonly border;
    private readonly editor;
    private readonly answers;
    private readonly selectedOptions;
    private page;
    private editorKind;
    private message;
    private finished;
    private _focused;
    constructor(options: QuestionnaireComponentOptions<QuestionId>);
    get focused(): boolean;
    set focused(value: boolean);
    render(width: number): string[];
    handleInput(data: string): void;
    invalidate(): void;
    dispose(): void;
    private handleEditorInput;
    private handlePageInput;
    private renderHintGroups;
    private movePage;
    private renderHeader;
    private renderTabs;
    private renderQuestion;
    private renderCustomOption;
    private renderReview;
    private moveSelection;
    private submitPage;
    private editNote;
    private beginEditor;
    private submitEditor;
    private advance;
    private hasReviewPage;
    private isReviewPage;
    private finish;
}
